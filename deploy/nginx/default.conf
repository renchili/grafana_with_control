server {
  listen 80;

  location / {
    proxy_pass http://grafana:3000;
    proxy_set_header Host $host;
  }

  location /api/platform/ {
    proxy_pass http://platform-api:8081;
    proxy_set_header Host $host;
  }
}
