# Grafana Control Plane App Plugin

A practical app plugin starter for:

- Drafts
- Conflict Resolve

This package is intended to be **dropped into an app plugin scaffold created by `@grafana/create-plugin`**, or used as a base source tree if you already have a Grafana plugin development environment.

See `INSTALL.md` for the recommended installation flow.
