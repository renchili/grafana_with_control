apiVersion: 1

apps:
  - type: 'rody-grafanacontrolplane-app'
    org_id: 1
    org_name: 'rody'
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
    secureJsonData:
      apiKey: secret-key
